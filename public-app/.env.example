# ===========================================
# Required for DB sync (no migrations - tables sync on server start)
# ===========================================
# PostgreSQL connection string. Create the database first, e.g.:
#   createdb shopify_app_db
DATABASE_URL=postgres://USER:PASSWORD@localhost:5432/shopify_app_db

# ===========================================
# Server
# ===========================================
NODE_ENV=development
PORT=3000

# ===========================================
# Shopify Public App (from Partner Dashboard)
# ===========================================
SHOPIFY_API_KEY=your_api_key
SHOPIFY_API_SECRET=your_api_secret
SCOPES=read_products,write_products,read_customers,write_customers,read_shop

# ===========================================
# App URLs (use ngrok or Cloudflare tunnel in development)
# For single-URL deployment (one tunnel): set BOTH to the same URL, e.g.:
#   FRONTEND_URL=https://xxx.trycloudflare.com
#   APP_URL=https://xxx.trycloudflare.com
# Then: build client (npm run build:client), start server, point tunnel to PORT.
# ===========================================
FRONTEND_URL=http://localhost:5173
APP_URL=http://localhost:3000

# ===========================================
# Email (optional - welcome email to admin on app install)
# ===========================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=
